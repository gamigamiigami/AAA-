# MICRO MANIA — Technical Architecture Contract

WarioWare / リズム天国 系の高速ミニゲーム集。ブラウザ単体・完全オフラインで動作する。

## 0. 決定事項サマリ

| 項目 | 決定 |
|---|---|
| 次元 / 路線 | 2D スタイライズ（Switch 系のポップで厚みのあるフラット + グラデーション） |
| 実行環境 | ブラウザ（Canvas 2D）。`index.html` を直接開くだけで動く |
| 外部依存 | **ゼロ**。npm パッケージ・CDN・外部画像・外部音源を一切使わない |
| アセット | 全て実行時の手続き生成（Canvas プリミティブ描画 + WebAudio 合成音） |
| 論理解像度 | 960 × 540 固定。レターボックスで DPR 対応スケーリング |
| 時間管理 | 固定タイムステップ 1/60 秒 + オーディオクロック駆動のビート同期 |

## 1. モジュール境界と所有権

クラシックスクリプト（ES module ではない）で `GG` 名前空間に登録する。
`file://` で直接開けることを最優先にしたため。

```
src/core/     … エンジン層。ゲーム内容を一切知らない
  util.js       数学・イージング・シード付き RNG
  canvas.js     描画ヘルパ G（角丸・星・アウトライン文字・影）
  input.js      キーボード / マウス / タッチの統一入力
  audio.js      WebAudio による BGM シーケンサと効果音の手続き生成
  fx.js         パーティクル・画面シェイク・フラッシュ・フローティングテキスト
  registry.js   ランタイムレジストリ（疎結合アクセス）+ ミニゲーム登録
  game.js       ディレクタ（ステートマシン・難易度・スコア・ライフ）
src/microgames/ … 1 ファイル 1 ミニゲーム。互いを一切参照しない
src/main.js     … ブート
```

**規約**

1. `src/microgames/*` は他のミニゲームを参照してはならない。
2. コア層はミニゲームを名指しで参照してはならない（登録は `GG.reg()` の一方向）。
3. モジュール間の共有は `GG.get('audio')` 等のレジストリ経由のみ。内部実装を直接掴まない。
4. グローバル汚染は `window.GG` 1 個だけ。

## 2. ミニゲーム API（この契約を破る変更は禁止）

```js
GG.reg({
  id:       'dodge',        // 一意
  verb:     'よけろ！',      // 画面に出る命令語
  verbEn:   'DODGE!',       // 併記される英語
  control:  'move',         // 'move' | 'press' | 'aim' | 'mash' | 'hold'
  beats:    8,              // 長さ（ビート数）
  boss:     false,
  bg:       ['#ff9f43', '#ee5253'],   // 背景グラデーション
  create(c) {               // c = ミニゲームコンテキスト
    return {
      update(dt) {},        // dt 秒。c.win() / c.lose() を呼ぶ
      draw(g) {}            // g = 描画ヘルパ
    };
  }
});
```

ミニゲームコンテキスト `c` が提供するもの:

| メンバ | 内容 |
|---|---|
| `c.rng` | **シード付き RNG。`Math.random()` の直接使用は禁止**（再現性のため） |
| `c.diff` | 1 / 2 / 3 の難易度段階 |
| `c.t` | 経過秒 |
| `c.timeLeft` | 残り秒 |
| `c.progress` | 0→1 の進行度 |
| `c.win()` / `c.lose()` | 勝敗確定（最初の 1 回のみ有効） |
| `c.input` | 入力 |
| `c.fx` | パーティクル等 |
| `c.sfx(name)` | 効果音 |
| `c.beat` | 現在のビート位置（float） |

時間切れ時の既定結果は `defaultResult`（省略時 `'lose'`）で指定する。

## 3. 再現性

乱数は全て `GG.U.RNG`（mulberry32）経由。ラウンドごとにシードをログに残せるため、
バグ報告・スクリーンショット比較で同じ状況を再現できる。
`Math.random()` はコードベース全体で使用禁止（唯一の例外はセッション初期シードの生成）。

## 4. タイミング設計

リズム天国水準の気持ちよさを狙い、**演出は全てビート同期**する。

- `audio.beat()` が AudioContext の時刻からビート位置を返す（音が鳴らない環境では `performance.now()` にフォールバック）。
- 命令語表示 = 2 ビート、ミニゲーム = 8 ビート（ボスは 16〜24）、結果表示 = 2 ビート。
- BPM はレベルとともに 132 → 最大 208 まで上昇。BGM も同時に速くなる。

## 5. 停止基準（プロトコル 6 章）

Pass 1 成立 → Pass 2 UX/視覚 → Pass 3 Critic 指摘修正。
改善幅が頭打ちになった時点で終了する。AAA 相当の物量は目標にしない。
